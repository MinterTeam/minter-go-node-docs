{
  "name": "minter-go-node-api",
  "version": "1.0.0",
  "dependencies": {
    "swagger-repo": "^2.0.0-rc.10",
    "json-merge-patch": "^0.2.3",
    "json-pointer": "^0.6.1"
  },
  "private": true,
  "scripts": {
    "start": "swagger-repo serve",
    "build": "swagger-repo build -o web_deploy",
    "test": "swagger-repo validate",
    "gh-pages": "swagger-repo gh-pages"
  }
}
