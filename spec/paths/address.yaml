get:
  summary: Get balance and transaction count of given address
  description: ''
  operationId: address
  produces:
    - application/json
  parameters:
    - name: address
      in: query
      description: 'Minter Address, starts with Mx...'
      required: true
      type: string
    - name: height
      in: query
      description: Specify height
      required: false
      type: string
  responses:
    '200':
      schema:
        $ref: '#/definitions/AddressResponse'
