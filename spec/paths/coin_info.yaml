get:
  summary: Get information about coin
  description: ''
  operationId: coin
  produces:
    - application/json
  parameters:
    - name: symbol
      in: query
      description: Coin symbol
      required: true
      type: string
    - name: height
      in: query
      description: Specify height
      required: false
      type: string
  responses:
    '200':
      schema:
        $ref: '#/definitions/CoinInfoResponse'
