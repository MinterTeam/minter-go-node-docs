get:
  summary: Get information about given block
  description: ''
  operationId: block
  parameters:
    - name: height
      in: query
      description: Block height
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/BlockResponse'
